# Compiled COBOL output
*.o
*.so
*.dylib

# cobc executables have no extension; keep them in bin/ or ignore by name
/bin/
payroll-v1
payroll-report
payroll-net
payroll-indexed
build-employee-index
tax-calc
hello

# Generated indexed master file (binary, GnuCOBOL/platform-specific) -
# rebuild with build-employee-index; see README Stage 4 instructions.
data/employees-indexed.dat

# macOS
.DS_Store
